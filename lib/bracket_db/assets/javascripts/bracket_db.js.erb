window.BracketDb = {
  find: function (handle) {
    return BracketDb.BRACKETS.find(function(bracket) {
      return bracket.handle == handle;
    });
  },
  BRACKETS: <%= Bracket.all.to_json(methods: :bracket_tree) %>
};
