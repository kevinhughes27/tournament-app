#= depend_on_file db/brackets.yml
#= depend_on_files db/brackets/*.json

Admin.BracketDb =
  BRACKETS: <%= Bracket.all.to_json %>
  find: (bracketName) ->
    _.find(Admin.BracketDb.BRACKETS, (bracket) -> bracket.name == bracketName)
