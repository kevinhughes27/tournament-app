<div class="register-box animated <%= login_animation %>">
  <% if session[:tournament_friendly_id].present? %>
    <%= render 'login_for_tournament' %>
  <% else %>
    <%= render 'login' %>
  <% end %>

  <br>
  <div class="text-center">
    <p>or</p>
  </div>

  <div class="social-auth-links text-center">
    <%= link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2), class: "btn btn-block btn-social btn-google btn-flat" %>
    <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook), class: "btn btn-block btn-social btn-facebook btn-flat" %>
  </div>
</div>
